# LangGraph核心依赖
langgraph>=1.0.1
langchain-core>=1.0.0
langsmith>=0.4.38

# Web框架和API
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0
pydantic>=2.5.0

# 数据处理和存储
redis>=5.0.0
# sqlite3 - 内置模块，无需安装
sqlalchemy>=2.0.0
# 注意：移除了PostgreSQL相关依赖（psycopg2等）以简化架构

# 异步和并发
asyncio
aiofiles>=23.0.0

# 测试框架
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# 监控和日志
prometheus-client>=0.19.0
structlog>=23.2.0

# 现有智能体系统依赖
# (这些将从现有代码中导入)